{
  "name": "bwa-task-list",
  "version": "1.0.0",
  "private": true,
  "description": "Hierarchical task list with drag and drop reordering",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run start:dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "test": "npm run test -w apps/api && npm run test -w apps/web",
    "test:e2e": "npm run test:e2e -w apps/api",
    "lint": "npm run lint -w apps/api && npm run lint -w apps/web",
    "typecheck": "npm run typecheck -w apps/api && npm run typecheck -w apps/web",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:reset": "npm run db:reset -w apps/api",
    "db:migrate": "npm run db:migrate -w apps/api",
    "db:seed": "npm run db:seed -w apps/api",
    "lint:fix": "npm run lint:fix -w apps/api && npm run lint -w apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.2.2"
  },
  "overrides": {
    "@nestjs/swagger": {
      "js-yaml": "^5.3.0"
    }
  }
}
